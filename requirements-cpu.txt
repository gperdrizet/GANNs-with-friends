# CPU-only requirements for GANNs-with-friends
# Use this if you do not have a GPU or want a smaller install

--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.0.1+cpu
torchvision==0.15.2+cpu

# Same as requirements.txt but without CUDA
Pillow>=9.0.0
PyYAML>=6.0
psycopg2-binary>=2.9.0
huggingface-hub>=0.20.0
